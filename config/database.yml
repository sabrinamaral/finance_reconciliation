default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: finance_reconciliation_development

test:
  <<: *default
  database: finance_reconciliation_test

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>
